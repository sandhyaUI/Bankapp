#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tick/bin/tickprocessor-driver.js" "$@"
  ret=$?
else 
  node  "$basedir/../tick/bin/tickprocessor-driver.js" "$@"
  ret=$?
fi
exit $ret
